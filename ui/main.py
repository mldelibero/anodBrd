#! /usr/bin/env python

#Run the rest of the program if the initialization is sucessful.
fro
state = state_t
state = init(state)

print "main time"
print state.ch1_time
print state.ch1_time

if (state != False):
    loop(state)
    postProcess()
